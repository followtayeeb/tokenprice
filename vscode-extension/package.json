{
  "name": "llm-costs-vscode",
  "displayName": "llm-costs — LLM Cost Estimator",
  "description": "Estimate and compare LLM API costs directly in VS Code. Select text, see token counts and pricing instantly.",
  "version": "0.1.0",
  "publisher": "followtayeeb",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/followtayeeb/llm-costs"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "llm",
    "tokens",
    "pricing",
    "cost",
    "openai",
    "anthropic",
    "claude",
    "gpt"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "llm-costs.estimateCost",
        "title": "llm-costs: Estimate Cost"
      },
      {
        "command": "llm-costs.compareModels",
        "title": "llm-costs: Compare Models"
      },
      {
        "command": "llm-costs.setModel",
        "title": "llm-costs: Set Default Model"
      }
    ],
    "keybindings": [
      {
        "command": "llm-costs.estimateCost",
        "key": "ctrl+shift+t",
        "mac": "cmd+shift+t",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "llm-costs",
      "properties": {
        "llm-costs.defaultModel": {
          "type": "string",
          "default": "claude-sonnet-4-5-20250514",
          "description": "Default model ID for cost estimation"
        },
        "llm-costs.showStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show token count and cost in the status bar when text is selected"
        },
        "llm-costs.autoEstimate": {
          "type": "boolean",
          "default": true,
          "description": "Automatically estimate cost when text is selected"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^2.22.0"
  }
}
